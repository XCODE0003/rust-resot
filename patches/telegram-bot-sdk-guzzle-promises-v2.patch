--- a/src/HttpClients/GuzzleHttpClient.php
+++ b/src/HttpClients/GuzzleHttpClient.php
@@ -4,8 +4,8 @@ use GuzzleHttp\Client;
 use GuzzleHttp\ClientInterface;
 use GuzzleHttp\Exception\GuzzleException;
 use GuzzleHttp\Exception\RequestException;
-use GuzzleHttp\Promise;
 use GuzzleHttp\Promise\PromiseInterface;
+use GuzzleHttp\Promise\Utils;
 use GuzzleHttp\RequestOptions;
 use Psr\Http\Client\RequestExceptionInterface;
 use Psr\Http\Message\ResponseInterface;
@@ -43,11 +43,18 @@ class GuzzleHttpClient implements HttpClientInterface
     }
 
     /**
      * Unwrap Promises.
-     * @throws Throwable
+     * Compatible with guzzlehttp/promises v2 (Utils::unwrap).
      */
     public function __destruct()
     {
-        Promise\unwrap(self::$promises);
+        if (empty(self::$promises)) {
+            return;
+        }
+        try {
+            Utils::unwrap(self::$promises);
+        } catch (Throwable $e) {
+            // Do not throw in __destruct - would crash the process
+        }
     }
 
     /**
